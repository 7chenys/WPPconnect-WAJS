# 下游发布说明

## 已发布的下游版本 4.6.1（2026-09-10；上游基线：4.6.0）

### 状态

此版本已发布为 `@yanmontain/wa-js@4.6.1`，包含由本仓库维护的下游兼容性补丁。它不是 WPPConnect WA-JS 的官方发布版本，也不得表述为上游已合并的内容。

该补丁是在排查与上游 PR [#3496](https://github.com/wppconnect-team/wa-js/pull/3496) 相关的建群运行时问题时制定的。本仓库不包含该 PR 的上游合并提交，也没有整体引入该 PR 的全部改动。

### 包含的改动

- 在发起原生建群请求前，解析 WhatsApp 内部的 `createGroup` 函数。
- 仅对该内部函数解析重试，最多 3 次，每次间隔 750 ms。
- 重试期间仅绕过相关查询缓存，防止 Meta 模块数量未变化时保留负向查询结果。
- 当模块仍在注册或找不到预期签名时，返回结构化的下游错误码。

### 明确未包含的改动

- 上游 PR #3496 中对全局 EventEmitter 的修改。
- 该 PR 中对全局定时器或大量注册器重试循环的修改。
- 任何声称此补丁已获得 WPPConnect Team、WhatsApp 或 Meta 官方支持的表述。

### 使用建议

将此构建视为临时的下游兼容版本。部署前必须针对目标 WhatsApp Web 运行时完成验证。只有在独立验证到合适的上游发布版本后，才能重新评估并移除或对齐此补丁。
